using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Dialogue
{
    // Class Dialogue, does not have to be monobehavior.
    // This stores all the information we need about a dialogue.
    //  - int DialogueType - type of dialogue
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
